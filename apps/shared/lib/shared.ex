defmodule Shared do
  @moduledoc """
  Geteilte Domain-Models für Kommunalpolitik-Tools.

  Enthält Modelle für Sitzung, Gremium, TOP, Dokument, Textblock,
  Abstimmung und Suchergebnis — unabhängig von spezifischen Tools
  wie `ratsinfo` oder `pdf_ingest`.

  Diese Structs spiegeln die Datenstruktur der RIS-API
  (risapi1.komuna.net/web/) wider.
  """
end
