defmodule Shared do
  @moduledoc """
  Geteilte Domain-Models für Kommunalpolitik-Tools.

  Enthält Modelle für Sitzung, Gremium, Tagesordnungspunkt (TOP),
  Dokument, Beschluss — unabhängig von spezifischen Tools wie
  `ratsinfo` oder `pdf_ingest`.
  """
end
